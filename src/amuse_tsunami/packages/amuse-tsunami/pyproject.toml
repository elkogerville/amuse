[project]
name = "amuse-tsunami"
version = "0.0.1"
# use the line below instead of the version above if this code is inside the AMUSE
# repository
# dynamic = ["version"]
requires-python = ">=3.7"
dependencies = [
    "amuse-framework"
]

[build-system]
requires = ["hatchling", "versioningit"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "versioningit"

[tool.versioningit.next-version]
method = "smallest"

[tool.versioningit.format]
distance = "{next_version}.dev{distance}+{vcs}{rev}"
dirty = "{base_version}+d{build_date:%Y%m%d}"
distance-dirty = "{next_version}.dev{distance}+{vcs}{rev}.d{build_date:%Y%m%d}"

[tool.versioningit.vcs]
method = { module = "version_helper", value = "get_amuse_version", module-dir = "amuse_tsunami/support/shared" }

[tool.hatch.build]
skip-excluded-dirs = true

[tool.hatch.build.targets.wheel]
include = ["amuse_tsunami/**/*.py"]
exclude = [
    "amuse_tsunami/packages",
    "amuse_tsunami/support",
    "amuse_tsunami/src",
    "amuse_tsunami/tests"
    ]
artifacts = ["amuse_tsunami/tsunami_worker"]

[tool.pytest.ini_options]
pythonpath = ["amuse_tsunami/tests/"]

testpaths = ["amuse_tsunami/tests"]
