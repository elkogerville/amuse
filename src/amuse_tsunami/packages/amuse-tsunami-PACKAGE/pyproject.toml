[project]
name = "amuse-tsunami-PACKAGE"
dynamic = ["version"]
requires-python = ">=3.7"
dependencies = [
    "amuse-framework==2025.9.1.dev23+g38aae9f45.d20251209",
    "amuse-tsunami"
]

[build-system]
requires = ["hatchling", "versioningit"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "versioningit"

[tool.versioningit.next-version]
method = "smallest"

[tool.versioningit.format]
distance = "{next_version}.dev{distance}+{vcs}{rev}"
dirty = "{base_version}+d{build_date:%Y%m%d}"
distance-dirty = "{next_version}.dev{distance}+{vcs}{rev}.d{build_date:%Y%m%d}"

[tool.versioningit.vcs]
method = { module = "version_helper", value = "get_amuse_version", module-dir = "amuse_tsunami/support/shared" }

[tool.hatch.build]
skip-excluded-dirs = true

[tool.hatch.build.targets.wheel]
include = ["amuse_tsunami"]
exclude = ["amuse_tsunami/*"]
artifacts = ["amuse_tsunami/tsunami_PACKAGE_worker"]

[tool.pytest.ini_options]
pythonpath = ["amuse_tsunami/tests/", "../../../../../tests"]

testpaths = ["amuse_tsunami/tests"]

